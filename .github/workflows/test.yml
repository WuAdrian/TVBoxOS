name: Test

on:
  workflow_dispatch:
    inputs:
      rebuild:
        description: '忽略构建记录以重新构建'
        required: false
        type: boolean
      donotpublish:
        description: '构建后不提交发布新版'
        required: false
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - userName: q215613905
            repoName: TVBoxOS
            branchName: main
            java_ver: '17'
            compile_sdk: '33'
            build_tools: '33.0.2'
    steps:
      - name: Checkout Main Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 关键1：使用GitHub官方方案配置Android+Java环境（替代手动配置，无路径问题）
      - name: Setup Official Android & Java Env
        uses: android-actions/setup-android@v3.0.0
        with:
          android-sdk-version: ${{ matrix.compile_sdk }}
          build-tools-version: ${{ matrix.build_tools }}
          java-version: ${{ matrix.java_ver }}

      # 关键2：强制接受所有Android许可证（官方环境仍需手动接受，避免卡壳）
      - name: Accept All Android SDK Licenses
        run: |
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager --list | grep -E "build-tools;${{ matrix.build_tools }}|platforms;android-${{ matrix.compile_sdk }}"

      # 步骤3：检查远端最新Commit（原有逻辑，无修改）
      - name: Check New Commit
        run: |
          upStream=https://github.com/${{ matrix.userName }}/${{ matrix.repoName }}
          echo "upStream=$upStream" >> $GITHUB_ENV
          commit=$(curl -sL $upStream/commits/${{ matrix.branchName }} |grep -o "/${{ matrix.userName }}/${{ matrix.repoName }}/commit/[a-z0-9]\+" |head -1 | cut -d\/ -f5)
          if [[ -z "${commit}" ]]; then
            commit=$(curl -s "https://api.github.com/repos/${{ matrix.userName }}/${{ matrix.repoName }}/commits/${{ matrix.branchName }}?per_page=1" | jq -r '.sha' )
          fi
          if ! grep -q "$commit" README.md || [ "${{ inputs.rebuild }}" == "true" ]; then
            echo "commit=$commit" >> $GITHUB_ENV
            echo "commitS=${commit:0:7}" >> $GITHUB_ENV
          fi
          echo "Latest Commit: $commit"

      # 步骤4：拉取源码+即时创建local.properties（时序最优，彻底解决文件缺失）
      - name: Checkout Source Code & Create local.properties
        if: ${{ env.commit }}
        run: |
          # 拉取TVBoxOS源码
          git clone ${{ env.upStream }} TVBoxOSC
          cd TVBoxOSC
          git checkout ${{ env.commit }}
          # 生成构建标签
          echo "tag=$(git log --date=format:'%Y%m%d-%H%M' --pretty=format:%cd ${{ env.commitS }} -1)" >> $GITHUB_ENV
          # 核心：拉取源码后立即创建local.properties，写入官方环境的SDK路径
          echo "sdk.dir=$ANDROID_HOME" > local.properties
          # 验证文件创建成功
          echo "=== local.properties 内容 ==="
          cat local.properties
          echo "============================"

      # 步骤5：源码修改（原有逻辑，无修改）
      - name: Extra Modify
        if: ${{ env.commit }}
        working-directory: TVBoxOSC
        run: |
          echo '修改VersionName'
          if [ "${{ matrix.userName }}" == "takagen99" ]; then
            sed -i 's/\.concat(buildTime())/ +\"${{ env.tag }}\"/g' app/build.gradle 
            sed -i "/android:text=/s#=\"#=\"${{ env.tag }}\\\\n\\\\n#" app/src/main/res/layout/dialog_about.xml
          else
             sed -i "/versionName/s#[0-9a-zA-Z_\.\'\"-]\+\$#\'${{ env.tag }}\'#" app/build.gradle
             sed -i "/android:text=/s#=\"#=\"${{ env.tag }}\\\\n\\\\n#" app/src/main/res/layout/dialog_about.xml
          fi 
          echo '添加crosswalk源，防挂'
          if grep -q 'crosswalk' build.gradle; then
            sed -i "/crosswalk/a\        maven { url 'https://o0halflife0o.github.io/crosswalk/releases/crosswalk/android/maven2' }" build.gradle
          else
            sed -i "/jitpack.io/a\        maven { url 'https://o0halflife0o.github.io/crosswalk/releases/crosswalk/android/maven2' }" build.gradle
          fi

      # 步骤6：压缩源码（原有逻辑，无修改）
      - name: Compress Source Code
        if: ${{ env.commit }}
        working-directory: TVBoxOSC
        run: |
          tar -cJf sourceCode-${{ env.commitS }}.tar.xz --exclude=.git --exclude=.github *

      # 步骤7：APK签名配置（原有逻辑，无修改）
      - name: Release Apk Sign
        if: ${{ env.commit }}
        working-directory: TVBoxOSC
        run: |
          signingConfigs=''
          signingConfig='ICAgICAgICAgICAgaWYgKHByb2plY3QuaGFzUHJvcGVydHkoIlJFTEVBU0VfU1RPUkVfRklMRSIpKSB7XAogICAgICAgICAgICAgICAgc2lnbmluZ0NvbmZpZyBzaWduaW5nQ29uZmlncy5teUNvbmZpZ1wKICAgICAgICAgICAgfVwK'
          signingConfigs="$(echo "$signingConfigs" |base64 -d )"
          signingConfig="$(echo "$signingConfig" |base64 -d )"
          sed -i -e "/defaultConfig {/i\\$signingConfigs " -e "/debug {/a\\$signingConfig " -e "/release {/a\\$signingConfig " app/build.gradle
          cp -f ${{ github.workspace }}/.github/workflows/TVBoxOSC.jks app/TVBoxOSC.jks
          sed -i '$a\RELEASE_STORE_FILE=./TVBoxOSC.jks'     ./gradle.properties
          sed -i '$a\RELEASE_KEY_ALIAS=TVBoxOSC'            ./gradle.properties
          sed -i '$a\RELEASE_STORE_PASSWORD=TVBoxOSC'       ./gradle.properties
          sed -i '$a\RELEASE_KEY_PASSWORD=TVBoxOSC'         ./gradle.properties
          sed -i 's/^#\(org.gradle.jvmargs=.*\)/\1/'        ./gradle.properties

      # 步骤8：初始化Gradle（原有逻辑，无修改）
      - uses: gradle/actions/setup-gradle@v4
        if: ${{ env.commit }}

      # 关键3：优化Gradle构建命令，增加全量日志+依赖刷新+错误兜底
      - name: Build Release APK With Gradle
        if: ${{ env.commit }}
        working-directory: TVBoxOSC
        run: |
          # 确保gradle脚本可执行
          chmod +x gradlew
          # 全参数构建，最大化暴露错误信息，便于后续排查
          ./gradlew assembleRelease \
            --parallel \
            --daemon \
            --warning-mode all \
            --stacktrace \
            --info \
            --no-build-cache \
            --refresh-dependencies \
            -Dorg.gradle.jvmargs=-Xmx2048m

      # 步骤9：整理APK文件（优化查找逻辑，避免全局搜索）
      - name: Prepare App Files
        if: ${{ env.commit }}
        working-directory: TVBoxOSC
        run: |
          rm -rf apk/ && mkdir -p apk/
          # 仅在项目内查找APK，指定类型为文件，避免目录干扰
          find app -name "*release*.apk" -type f | while read file; do
            base=$(basename "$file")
            if [[ "$base" == TVBox_* ]]; then
              new_base=$(echo "$base" | sed "s/release/${{ matrix.userName }}_${{ env.tag }}/")
            else
              new_base="TVBox_${{ matrix.userName }}_${{ env.tag }}.apk"
            fi
            mv "$file" "apk/$new_base"
            echo "已找到并重命名APK：$file -> apk/$new_base"
          done
          # 移动源码压缩包
          mv sourceCode-${{ env.commitS }}.* apk/

      # 步骤10：生成发布日志（原有逻辑，无修改）
      - name: Generate Release Note
        if: ${{ env.commit }}
        working-directory: TVBoxOSC
        run: |
          lastCommit=$(grep "${{ matrix.userName }}\/${{ matrix.repoName }}" ${{ github.workspace }}/README.md |grep -o '[a-z0-9]\{40\}')
          export LESSCHARSET=utf-8
          echo -e "Credit: [${{ matrix.userName }}](${{ env.upStream }})\nCommit: ${{ env.commit }}\nChangelog:\n\`\`\`" > apk/${{ matrix.userName }}-Release.log
          if [ "${{ env.commit }}" == "${lastCommit}" ]; then
            git log --pretty=format:%B ${{ env.commitS }} -1 |sed -e "s# \{2,\}#\n#g" -e "/^Merge \(pull\|branch\|remote\)/d" -e '/^$/d' |cat -n |sort -k2,2 -k1,1n |uniq -f1 |sort -k1,1n |cut -f2- >> apk/${{ matrix.userName }}-Release.log
          else
            git log --pretty=format:%B ${{ env.commitS }}...${lastCommit:0:7} |sed -e "s# \{2,\}#\n#g" -e "/^Merge \(pull\|branch\|remote\)/d" -e '/^$/d' |cat -n |sort -k2,2 -k1,1n |uniq -f1 |sort -k1,1n |cut -f2- >> apk/${{ matrix.userName }}-Release.log
          fi
          echo -e '\n```' >> apk/${{ matrix.userName }}-Release.log

      # 步骤11：上传构建产物（原有逻辑，无修改）
      - name: Upload App To Artifact
        uses: actions/upload-artifact@v4
        if: ${{ env.commit }}
        with:
          name: ${{ matrix.userName }}-${{ matrix.repoName }}-${{ env.tag }}
          path: TVBoxOSC/apk/*

      # 步骤12：发布控制（原有逻辑，无修改）
      - name: Skip Publish Flag
        if: ${{ inputs.donotpublish && env.commit }}
        run: |
          echo "commit=" >> $GITHUB_ENV

  # 清理任务（原有逻辑，无修改）
  clean:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Delete Older Workflow Runs
        uses: Mattraks/delete-workflow-runs@main
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 14
          keep_minimum_runs: 10
